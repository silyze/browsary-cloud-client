{
  "name": "@silyze/browsary-cloud-client",
  "version": "1.1.1",
  "description": "Browser provider for Browsary Cloud instances",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [],
  "author": "Mihail Mojsoski",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^22.15.30",
    "puppeteer-core": "^24.10.0",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "watch": "npx tsc --watch",
    "build": "npx tsc",
    "prepare": "npm run build",
    "version": "git add -A lib",
    "postversion": "git push && git push --tags"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@mojsoski/assert": "^1.0.0",
    "@silyze/browser-provider": "^1.0.0",
    "@silyze/coordinator": "^1.0.0"
  }
}
